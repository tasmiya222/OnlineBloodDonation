@model IEnumerable<OnlineBloodBank.Models.RequestType>

@{
    ViewBag.Title = "AllRequesttype";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="row">
    <div class="col-lg-9">
        <h2 class="title-1 m-b-25">All Request Type</h2>
        <div style="margin-left:90%;margin-bottom:2%">

                @Html.ActionLink("Create New", "Create",null, new { @class= "btn btn-secondary" })

        </div>
        <div class="table-responsive table--no-card m-b-40">
            <table class="table table-borderless table-striped table-earning">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.RequestTypes)
                        </th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.RequestTypes)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.RequestTypeID }, new { @class= "btn btn-info" }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.RequestTypeID } , new { @class= "btn btn-danger" })
                            </td>
                        </tr>
                    }
                    </tbody>
            </table>
        </div>
    </div>
  
</div>

@*<h2>AllRequesttype</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RequestTypes)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RequestTypes)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.RequestTypeID }) |
                @Html.ActionLink("Details", "Details", new { id=item.RequestTypeID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.RequestTypeID })
            </td>
        </tr>
    }

    </table>*@
